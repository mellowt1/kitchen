{
  "name": "kitchen",
  "private": true,
  "type": "module",
  "description": "Paul's Kitchen: the week's dinners, recipes, shopping and pizza dough. A static PWA that syncs through the paul-hub Worker.",
  "scripts": {
    "test": "node --test test/logic.test.mjs",
    "icons": "node scripts/make-icons.mjs",
    "serve": "node scripts/serve.mjs",
    "shots": "node scripts/shots.mjs",
    "sync-check": "node scripts/sync-check.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "playwright": "^1.63.0"
  }
}
